// Shared class strings for the chat surface's small controls.

/** Soft pebble button in the composer's control row. */
export const composerChip =
  "flex items-center gap-1.5 rounded-full border border-[var(--color-border)] px-2.5 py-1 text-[11.5px] font-medium text-[var(--color-text-muted)] transition hover:bg-[var(--color-surface-2)] hover:text-[var(--color-text)]";

/** Row inside a dropdown popover. */
export const menuItem =
  "flex w-full cursor-pointer items-center gap-2 rounded-lg px-2 py-1.5 text-[12.5px] outline-none transition data-[highlighted]:bg-[var(--color-surface-2)]";

/** Dropdown popover container (liquid glass). */
export const menuContent =
  "glass-strong z-50 rounded-[18px] border border-[var(--glass-border)] p-1.5 animate-[popIn_140ms_var(--ease-natural)]";
