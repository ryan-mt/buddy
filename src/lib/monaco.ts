// Wires Monaco to the locally-bundled `monaco-editor` package and its web
// workers (imported via Vite's `?worker`), so the editor runs fully offline
// instead of fetching from a CDN. Imported for side effects by the editor pane.

import * as monaco from "monaco-editor";
import { loader } from "@monaco-editor/react";
import editorWorker from "monaco-editor/esm/vs/editor/editor.worker?worker";
import jsonWorker from "monaco-editor/esm/vs/language/json/json.worker?worker";
import cssWorker from "monaco-editor/esm/vs/language/css/css.worker?worker";
import htmlWorker from "monaco-editor/esm/vs/language/html/html.worker?worker";
import tsWorker from "monaco-editor/esm/vs/language/typescript/ts.worker?worker";

const env: monaco.Environment = {
  getWorker(_id, label) {
    switch (label) {
      case "json":
        return new jsonWorker();
      case "css":
      case "scss":
      case "less":
        return new cssWorker();
      case "html":
      case "handlebars":
      case "razor":
        return new htmlWorker();
      case "typescript":
      case "javascript":
        return new tsWorker();
      default:
        return new editorWorker();
    }
  },
};

(self as typeof globalThis & { MonacoEnvironment: monaco.Environment }).MonacoEnvironment = env;

loader.config({ monaco });
